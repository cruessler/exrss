language: elixir
elixir: '1.6.4'
otp_release: '20.0'

services:
  - postgresql

addons:
  postgresql: '9.6'

before_script:
  - cp config/test.travis.exs config/test.exs
  - psql -c 'create database travis_ci_test;' -U postgres
